
/**
 * For In Loops:
 * Great for counting k:v pairs in an object
 * iterates over the emumerable properties
 * 
 * for (const key in object) {
    if (object.hasOwnProperty(key)) {
        const element = object[key];
         }
 */
